# Required in production for canonical URLs, sitemap and social metadata.
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Optional. Public GitHub API requests work without a token, but a token increases rate limits.
GITHUB_TOKEN=
